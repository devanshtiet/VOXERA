# Git Operations Rule
- **NEVER** execute `git push` to the `main` or `master` branch under any circumstances without first asking for and receiving explicit, written confirmation from the user.
- All code changes must either remain unpushed, or be pushed to a feature branch, unless the user explicitly dictates otherwise.
